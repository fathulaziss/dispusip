import 'package:flutter/material.dart';

class HistoryContentTwo extends StatelessWidget {
  const HistoryContentTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
