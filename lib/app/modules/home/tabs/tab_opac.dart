import 'package:flutter/material.dart';

class TabOpac extends StatelessWidget {
  const TabOpac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tab Opac is working', style: TextStyle(fontSize: 20)),
    );
  }
}
